<div id="home">
  <nav class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="row">
          <div class="col-lg-6 d-flex align-items-center">
            <img class="popcorn" src="../assets/images/popcorn.png" alt="Popcorn">
            <h1 a href="/index">DASFLIX</h1>
          </div>
          <div class="col-lg-6 d-flex align-items-center justify-content-end">
            {{#if user}}
            <a href="/watchlists" class="btn btn-primary">Watchlist</a>
            <a href="/logout" class="btn btn-primary">Sign Out</a>
            {{else}}
            <p>Please <a href="/login">Sign in</a>.</p>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </nav>
  <section class="container-fluid p-5">
    <div id="showcase">

    </div>
    <div  id="user-info" data-user="{{user.id}}" class="row p-5" id="filters">
      <input type="text" placeholder="Search Movie/TV" id="submit-query">
      <input type="submit" id="submit">
    </div>

    <div id="search">

    </div>
    <!-- Modal HTML embedded directly into document -->
    <div id="ex1" class="modal list-group">

    </div>

    <!-- Link to open the modal -->
    {{!-- <p><a href="#ex1" rel="modal:open">Open Modal</a></p> --}}
  </section>


</div>