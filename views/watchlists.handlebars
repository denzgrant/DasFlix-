<div id="home">
    <nav class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="row">
                    <div class="col-lg-6 d-flex align-items-center">
                        <img class="popcorn" src="../assets/images/popcorn.png" alt="Popcorn">
                        <h1>DASFLIX</h1>
                    </div>
                    <div class="col-lg-6 d-flex align-items-center justify-content-end">
                        {{#if user}}
                        <h2>This is our Home Page!</h2>
                        <a href="/" class="btn btn-primary">Home</a>
                        <a href="/login" class="btn btn-primary">Log Out</a>
                        {{else}}
                        <a href="/" class="btn btn-primary">Home</a>
                        <p>Please <a href="/login">Log In</a>.</p>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </nav>
    <section id="placeholder" class="container-fluid p-5">
        <a data-user="{{user.id}}"href="#create-watchlist" id="watchlist-view" class="btn btn-primary mb-5">Create New Watchlist</a>
        <ul class="watchlists-grid" id="watchlists">

            {{!-- List box that can be reused in index --}}
            <li class="watchlist-box d-flex flex-column justify-content-around align-items-center">
                <div>
                    <h2>Example Watchlist</h2>
                </div>
                <div class="d-flex">
                    <a href="#view" id="watchlist-view" class="btn btn-primary">View Watchlist</a>
                    <a href="#remove" id="watchlist-view" class="btn btn-primary">Remove Watchlist</a>
                </div>
            </li>

        </ul>

    </section>
</div>